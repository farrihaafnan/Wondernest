{
  "name": "e2e-tests",
  "version": "1.0.0",
  "description": "E2E tests for WonderNest project using Playwright",
  "scripts": {
    "test": "npx playwright test",
    "prepare": "npx playwright install --with-deps"
  },
  "devDependencies": {
    "@playwright/test": "^1.41.2"
  }
} 